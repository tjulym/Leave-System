import { Degree } from "./degree";

export class User {
    worker_address: string; 
    worker_degree: Degree[];
    worker_email: string; 
    worker_id: string; 
    worker_male: boolean; 
    worker_name: string; 
    worker_workAdd_time: number;
    worker_year_holidays_residue: number; 
    worker_year_holidays_used: number
}